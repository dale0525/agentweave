{
  "name": "@agentweave/cloudflare-model-gateway",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/*.js && node --test test/*.test.js",
    "test": "node --test test/*.test.js"
  }
}
