{
  "name": "@agentweave/cloudflare-entitlement-policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/*.js && node --test test/*.test.js",
    "test": "node --test test/*.test.js"
  }
}
