# App-local packages

Place app-specific Skill or Connector packages here when they are ready to be validated by the AgentWeave package lifecycle.

Do not store API keys, refresh tokens, passwords, or other credentials in this directory. Authentication belongs in the host credential vault.
